namespace E_POS.Infrastructure.Integrations.ProductLookup;

/// <summary>
/// Marker for future approved external product-data provider adapters (B6).
/// No production provider is registered. Coordinator resolves zero providers → NO_MATCH.
/// Do not invent vendor adapters without licensing/credentials approval.
/// </summary>
public static class ProductLookupIntegrationMarker
{
}
